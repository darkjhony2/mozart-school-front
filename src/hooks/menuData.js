import React, { useEffect } from 'react'

const useMenuData = props => {


    useEffect(() => {
        init();
    }, [])
    
    function init() {
        let token = await 
        localStorage.setItem('')
    }

  return (
    <div>menuData</div>
  )
}

export default useMenuData